{
  "name": "@jupyterlab/example-federated",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "build": "cd core_package && npm run build && cd ../md_package && npm run build"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "workspaces": ["*"]
}
